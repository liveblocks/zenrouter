---
title: Zen Router
description: An opinionated HTTP router with typed path params, built-in body validation, and a clean auth model.
---

Zen Router is an opinionated API router built by
[Liveblocks](https://liveblocks.io), where it has been powering billions of
requests per month. It's designed for Cloudflare Workers, Bun, Node.js, and
every other modern JavaScript runtime.

```bash tab="npm"
npm install @liveblocks/zenrouter
```

```bash tab="pnpm"
pnpm add @liveblocks/zenrouter
```

```bash tab="bun"
bun add @liveblocks/zenrouter
```

```bash tab="yarn"
yarn add @liveblocks/zenrouter
```

## Quick example

```ts tab="Full"
import { ZenRouter } from "@liveblocks/zenrouter";
import { z } from "zod";

const zen = new ZenRouter({
  authorize: async (req) => {
    const token = req.headers.get("Authorization");
    const currentUser = await db.getUserByToken(token);
    if (!currentUser) return false;
    return { currentUser };
  },
});

// Get a specific post
zen.route(
  "GET /api/posts/<postId>",

  async ({ p, auth }) => {
    const post = await db.getPost(auth.currentUser.id, p.postId);
    return { id: post.id, title: post.title };
  }
);

// Create a new post for the current user
zen.route(
  "POST /api/posts",

  z.object({ title: z.string() }),

  async ({ auth, body }) => {
    const post = await db.createPost({
      title: body.title,
      authorId: auth.currentUser.id,
    });
    return { id: post.id, title: post.title };
  }
);

export default zen;
```

```ts tab="Auth"
import { ZenRouter } from "@liveblocks/zenrouter";
import { z } from "zod";

const zen = new ZenRouter({
  // [!code highlight]
  authorize: async (req) => {
    const token = req.headers.get("Authorization"); // [!code highlight]
    const currentUser = await db.getUserByToken(token); // [!code highlight]
    if (!currentUser) return false; // [!code highlight]
    return { currentUser }; // [!code highlight]
  }, // [!code highlight]
});

// Get a specific post
zen.route(
  "GET /api/posts/<postId>",

  // [!code highlight]
  async ({ p, auth }) => {
    const post = await db.getPost(auth.currentUser.id, p.postId); // [!code highlight]
    return { id: post.id, title: post.title };
  }
);

// Create a new post for the current user
zen.route(
  "POST /api/posts",

  z.object({ title: z.string() }),

  // [!code highlight]
  async ({ auth, body }) => {
    const post = await db.createPost({
      title: body.title,
      // [!code highlight]
      authorId: auth.currentUser.id,
    });
    return { id: post.id, title: post.title };
  }
);

export default zen;
```

```ts tab="Route params"
import { ZenRouter } from "@liveblocks/zenrouter";
import { z } from "zod";

const zen = new ZenRouter({
  authorize: async (req) => {
    const token = req.headers.get("Authorization");
    const currentUser = await db.getUserByToken(token);
    if (!currentUser) return false;
    return { currentUser };
  },
});

// Get a specific post
zen.route(
  // [!code highlight]
  "GET /api/posts/<postId>",

  // [!code highlight]
  async ({ p, auth }) => {
    const post = await db.getPost(auth.currentUser.id, p.postId); // [!code highlight]
    return { id: post.id, title: post.title };
  }
);

// Create a new post for the current user
zen.route(
  "POST /api/posts",

  z.object({ title: z.string() }),

  async ({ auth, body }) => {
    const post = await db.createPost({
      title: body.title,
      authorId: auth.currentUser.id,
    });
    return { id: post.id, title: post.title };
  }
);

export default zen;
```

```ts tab="zod"
import { ZenRouter } from "@liveblocks/zenrouter";
// [!code highlight]
import { z } from "zod";

const zen = new ZenRouter({
  authorize: async (req) => {
    const token = req.headers.get("Authorization");
    const currentUser = await db.getUserByToken(token);
    if (!currentUser) return false;
    return { currentUser };
  },
});

// Get a specific post
zen.route(
  "GET /api/posts/<postId>",

  async ({ p, auth }) => {
    const post = await db.getPost(auth.currentUser.id, p.postId);
    return { id: post.id, title: post.title };
  }
);

// Create a new post for the current user
zen.route(
  "POST /api/posts",

  // [!code highlight]
  z.object({ title: z.string() }),

  async ({ auth, body }) => {
    const post = await db.createPost({
      title: body.title,
      authorId: auth.currentUser.id,
    });
    return { id: post.id, title: post.title };
  }
);

export default zen;
```

```ts tab="decoders"
import { ZenRouter } from "@liveblocks/zenrouter";
// [!code highlight]
import { object, string } from "decoders";

const zen = new ZenRouter({
  authorize: async (req) => {
    const token = req.headers.get("Authorization");
    const currentUser = await db.getUserByToken(token);
    if (!currentUser) return false;
    return { currentUser };
  },
});

// Get a specific post
zen.route(
  "GET /api/posts/<postId>",

  async ({ p, auth }) => {
    const post = await db.getPost(auth.currentUser.id, p.postId);
    return { id: post.id, title: post.title };
  }
);

// Create a new post for the current user
zen.route(
  "POST /api/posts",

  // [!code highlight]
  object({ title: string }),

  async ({ auth, body }) => {
    const post = await db.createPost({
      title: body.title,
      authorId: auth.currentUser.id,
    });
    return { id: post.id, title: post.title };
  }
);

export default zen;
```

```ts tab="CORS"
import { ZenRouter } from "@liveblocks/zenrouter";
import { z } from "zod";

const zen = new ZenRouter({
  authorize: async (req) => {
    const token = req.headers.get("Authorization");
    const currentUser = await db.getUserByToken(token);
    if (!currentUser) return false;
    return { currentUser };
  },
  // [!code highlight]
  cors: true,
});

// Get a specific post
zen.route(
  "GET /api/posts/<postId>",

  async ({ p, auth }) => {
    const post = await db.getPost(auth.currentUser.id, p.postId);
    return { id: post.id, title: post.title };
  }
);

// Create a new post for the current user
zen.route(
  "POST /api/posts",

  z.object({ title: z.string() }),

  async ({ auth, body }) => {
    const post = await db.createPost({
      title: body.title,
      authorId: auth.currentUser.id,
    });
    return { id: post.id, title: post.title };
  }
);

export default zen;
```

## Features

- [Type-safe everywhere](/docs/routes). Path params (`p`), query strings (`q`), and request bodies (`body`) are all fully typed.
- [Body validation](/docs/routes#body-validation). With any [Standard Schema](https://standardschema.dev/) compatible library.
- [Authorization](/docs/authorization). Mandatory by design, opt-out for public routes.
- [Composing routers](/docs/composing-routers). Isolate routers by auth strategy with `ZenRelay`.
- [CORS](/docs/cors). Built-in, not bolted on.
- [Error handling](/docs/todo). `abort(404)` from any handler, customizable error shapes.
- [Response helpers](/docs/todo). `json()`, `html()`, `empty()`, `textStream()`, and more.
- [OpenTelemetry](/docs/opentelemetry). Automatic span attributes for matched routes.
- [Runs anywhere](/docs/supported-runtimes). Cloudflare Workers, Bun, Node.js, and any runtime with Web `Request`/`Response`.
